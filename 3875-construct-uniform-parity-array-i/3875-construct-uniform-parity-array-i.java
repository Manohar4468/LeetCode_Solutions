class Solution {
    public boolean uniformArray(int[] nums1) {
        /*
        ->if all elements in nums1 are even i will simply return true;
        ->if nums1 contain atleast one odd i will make all elemenst as odd (even-odd=odd)
        */
        return true;
    }
}